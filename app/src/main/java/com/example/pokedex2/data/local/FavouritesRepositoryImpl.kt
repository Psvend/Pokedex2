package com.example.pokedex2.data.local

import android.content.Context
import com.example.pokedex2.model.Affirmation
import kotlinx.coroutines.flow.Flow
import javax.inject.Inject

/*
class FavouritesRepositoryImpl @Inject constructor(
    private val context: Context
) : FavouritesRepository {

    override fun getFavourites(): Flow<List<Affirmation>> {
        return getFavouriteAffirmations(context)
    }

    override suspend fun addFavourite(affirmation: Affirmation) {
        saveFavouritePokemon(context, affirmation)
    }

    override suspend fun removeFavourite(affirmationId: Int) {
        removeFavouriteAffirmation(context, affirmationId)
    }
}

 */

